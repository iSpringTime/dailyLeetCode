cmake_minimum_required(VERSION 3.14)
set(target pro1)
link_directories(./lib)
include_directories(../)
set(CMAKE_C_STANDARD 99)
set(srcs .)
aux_source_directory(. srcs)
add_executable(${target} ${srcs})
#target_link_libraries(${target} securec)r